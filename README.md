# Переполох во дворе

Парковочная головоломка для Яндекс Игр: выведи «жигулёнок» деда через ворота заставленного сельского двора. 128 уровней в десяти главах (число растёт: уровни добавляются в середину кампании), авторская механика «Живой двор» с нажимными кнопками и замками, боссы, ежедневный уровень, недельные кубки, ежедневный подарок, 18 достижений, 384 звезды, двор с новой визуальной стадией каждые 10 уровней и отдельными наградами за звёзды, скины, локализация ru/en/tr, два лидерборда и полное управление Android TV-пультом.

## Запуск

```bash
npm install
npm run dev        # http://localhost:5173 (mock-режим, без Яндекс SDK)
```

Полезные параметры URL: `?mock=1` — принудительный mock-режим только в dev/e2e-сборках; `?tv=1` — эмуляция TV-устройства только вместе с mock для QA; `?lang=ru|en|tr` — QA-переопределение языка (на первом запуске язык берётся у платформы); `?daytime=day` — фиксированная дневная палитра для скриншотов. `?qaTools=1` в dev/e2e добавляет в настройки кнопку 🛠: она открывает всю кампанию, Высшую лигу и Endless в изолированном сеансе без записи прогресса, рейтингов и рекламы. В активном QA-режиме `?qaYard=0|20|40|60|80|100` позволяет отдельно сравнивать состояния двора, а кнопка 🧱 в настройках открывает редактор уровней (JSON + превью, валидатор, решатель, оценка сложности, поиск дубликатов, тестовый запуск). В production mock- и QA-код недоступен. В консоли Яндекс Игр создайте таблицы `yardstars` (сумма звёзд) и `dailystreak` (серия дней), обе — «больше лучше».

Аналитика воронки отправляется в счётчик Яндекс Метрики и включается только сборкой с `VITE_YM_COUNTER_ID` (например `VITE_YM_COUNTER_ID=12345678 npm run build`). Без переменной адаптер выключен целиком: внешних запросов нет, домен Метрики в бандл не попадает. Отправляются только факты воронки (старт/победа уровня, попытка, подсказка, реклама, daily, метапрогрессия) — без персональных данных и идентификаторов; вебвизор, карта кликов, отслеживание ссылок, hash-переходов, точный отказ и e-commerce отключены явно при инициализации счётчика (`src/platform/yandex.ts`). QA-сеанс (`?qa=1`) в воронку не пишет.

Remote Config читает необязательные флаги `interstitial_every`, `interstitial_min_level`, `interstitial_min_session_ms`, `free_hints_per_session`. Без них используются безопасные значения 6 / 10 / 240000 / 1: за полную кампанию возможно не более 11 interstitial, и ни одного в первые четыре минуты сессии.

Если SDK Яндекс Игр не загрузился или упал на инициализации, игра не останавливается — включается локальный fallback (`src/platform/local-fallback.ts`): сохранение в localStorage, без рекламы и лидербордов, ненавязчивое уведомление игроку. `?sdkFail=1` (только dev/e2e) эмулирует этот сценарий для проверки.

Тяжёлые solver-тесты (проверка оптимума решателем для всех уровней кампании) вынесены из `npm test` в отдельную команду `npm run test:solver` — обе входят в `npm run check`.

## Команды

| Команда | Что делает |
|---|---|
| `npm run dev` | дев-сервер Vite |
| `npm run typecheck` | строгая проверка типов |
| `npm test` | unit-тесты (логика, SDK, сохранения, daily и вся кампания) |
| `npm run solve` | отчёт решателя: оптимум каждого уровня, сверка с `par` |
| `npm run rebalance` | детерминированно применяет проверенный ребаланс кампании и пересчитывает `par/par2` решателем |
| `npm run build` | production-сборка в `dist/` |
| `npm run e2e` | собирает специальную e2e-версию с mock SDK и запускает браузерные тесты Playwright |
| `npm run screenshots:yandex` | снимает два desktop- и два mobile-геймплейных кадра; нужен запущенный preview |
| `npm run generate -- [seed] [iter]` | поиск новых сложных уровней перебором |

Адаптивный e2e-прогон проверяет меню и активный уровень на десяти размерах от 320×568 до 1920×1080, портретную и альбомную ориентации, поворот экрана без перезагрузки, touch- и mouse-управление, отсутствие системной прокрутки и минимальную touch-зону 44×44 px. TV-сценарии отдельно проходят меню и первый уровень только событиями `Arrow*`, `Enter`, `HISTORY_BACK` и `EXIT`.

## Архив для Яндекс Игр

```bash
npm run build
cd dist && tar -a -c -f ../game.zip *   # или заархивируйте содержимое dist/ любым способом
```

Production использует только официальный относительный `/sdk.js`; абсолютных S3/CDN URL и mock-рекламы в архиве нет. `index.html` лежит в корне архива, игровые ассеты используют относительные пути (`base: './'`), а legacy-бандл обеспечивает запуск в заявленных Яндексом старых браузерах. Локальная разработка без SDK работает только в dev/e2e-режимах.

## Промо-материалы для каталога

`npm run promo:puzzle` — повторно нарезает финальные материалы из `ai-cover-puzzle-v2-src.png`: обложку `ai-cover-puzzle-v2-800x470.png` и иконку `ai-icon-puzzle-v2-512.png`. Устаревшие варианты удалены, чтобы в Консоль нельзя было случайно загрузить неверный файл.

`npm run screenshots:yandex` — переснимает реальные экраны в форматах 720×1280 и 1280×720. `npm run verify:dist` дополнительно проверяет размеры и 24-разрядный RGB-формат шести файлов, выбранных для публикации.

## Добавление уровня

Уровни лежат в `src/levels/levels.json` — код менять не нужно. После добавления запустите `npm run solve`: он покажет оптимум (`par`) и проверит валидность/проходимость. Тесты дополнительно требуют, чтобы после обучения в оптимальном маршруте участвовала минимум половина препятствий. Категории означают реальную сложность: easy ≤ 5 ходов, medium 6–10, hard ≥ 11.

## Высшая лига двора (пост-кампания)

После первого прохождения уровня 100 показывается финальная сцена (один раз, флаг `endingSeen`) и открывается **Высшая лига**: 30 мастер-испытаний на базе лучших уровней кампании с модификаторами (без подсказок / без отмены) и медалями бронза/серебро/золото. Медали дают очки лиги, очки — ранги (Новичок → Легенда двора). За прохождение кампании выдаётся легендарный скин.

Ключевое решение: **очки лиги деривируются из медалей** (сумма), медали в сейве хранятся как максимум по каждому испытанию — награда физически не может быть выдана повторно (перезагрузка/облачный merge/переигровка идемпотентны). Испытания ссылаются на `sourceLevelId`, пороги считаются из `par`/`par2`, поэтому золото всегда достижимо (проверяется тестом решателем).

Модули: `src/game/elite.ts` (медали/очки/ранги), `src/levels/elite-challenges.ts` (30 испытаний), `src/ui/campaign-ending.ts` (финальная сцена); экраны лиги — в `src/ui/app.ts` (тем же паттерном, что и остальные экраны).

**Лидерборд `eliteweekly`** (Stage B, недельный чемпионат) — реализован: `src/game/elite-weekly.ts` (FNV-1a по неделе, `weeklyScore` = медаль×1000 − ходы), карточка на экране лиги, доска `eliteweekly` для сравнения (см. `src/platform/types.ts`). Доска **опциональна**: без таблицы в консоли чемпионат работает как еженедельное испытание с локальным рекордом — игра не ломается.

## Структура

- `src/core/` — чистая игровая логика: правила движения, BFS-решатель, валидатор
- `src/levels/levels.json` — данные уровней кампании (сейчас 128)
- `src/platform/` — интерфейс `Platform`: mock (localStorage, фейковая реклама) и Яндекс SDK
- `src/ui/` — SVG-поле с drag-управлением, экраны, двор-меню
- `src/game/` — звёзды/прогрессия двора, daily Worker, сохранения, WebAudio-звук
- Проектная документация: `PRODUCT_SPEC.md`, `GAME_DESIGN.md`, `TECHNICAL_DESIGN.md`, `IMPLEMENTATION_PLAN.md`

## Живой двор и дед (комедийный слой)

Двор реагирует на игрока, а **дед** — главный персонаж-комментатор: SVG-портрет со сменой настроения и авто-скрывающийся пузырь реплики (`src/ui/yard-reactions.ts`), питаемый чистой data-driven системой реплик (`src/game/grandpa.ts`, тексты — ключи `grandpa.*` ru/en/tr). Реплики ограничены кулдауном/анти-повтором, замолкают при паузе/рекламе, уважают `prefers-reduced-motion` и служат субтитром при выключенном звуке. Настройка «Живой двор» в меню. Визуальные реакции окружения уже в `BoardView`.

**Боссы** (`src/game/boss.ts`, `BOSS_SYSTEM.md`): 5 сюжетных многофазных боссов (слоты 10/25/50/75/100), **все играбельны** — интро деда, HUD «Фаза N из M», переходы фаз без перезагрузки, уникальная победа. Первый дивизион Высшей лиги доступен с позиции 65, а финальный босс 100 открывает остальные дивизионы, недельный чемпионат и Испытания деда. Каждая фаза — обычный solver-проверяемый под-уровень (ноль изменений core/solver). Прогресс кампании и награды пишутся только после полной победы, повтор не выдаёт награды заново. Требования к звуку — `AUDIO_ASSETS_REQUIRED.md`. Передача проекта — `PROJECT_HANDOFF.md`.

## Первая сессия и полировка онбординга

Первые 10 уровней и первые минуты игры отполированы отдельно (`FIRST_SESSION_DESIGN.md`, `UX_ACCEPTANCE_CHECKLIST.md`). Ключевые фиксы: приветствие деда больше не наслаивается на обучающий hint-toast (отложено до его исчезновения); подсказка на уровнях 1-3 бесплатна и не тратит платный токен; onboarding-стрелка получила статичный вариант для `prefers-reduced-motion`. Debug-режимы `?grandpaDebug=1` и `?analyticsDebug=1` работают только в dev/e2e (гейт по `import.meta.env`), в production отсутствуют независимо от query-строки.

Внутренняя аналитика-заглушка (`src/game/analytics.ts`) — типизированные события воронки (`game_start`…`returned_to_menu`), no-op по умолчанию, без персональных данных; готова к подключению реальной платформы позже.

Звук: централизованный реестр сэмплов (`src/game/sound-registry.ts`) с graceful fallback на синтез — см. `AUDIO_ASSETS_REQUIRED.md`.
